# tonghuashunAI 的增量依赖
# 基础依赖装 vendor/Qbot/requirements.txt，这里只列 Qbot 未涵盖或版本要求不同的
# 装法：
#   pip install -r vendor/Qbot/requirements.txt
#   pip install -r requirements.txt

# 数据源（Qbot 已依赖 akshare，此处显式钉版本以免下游变动）
akshare>=1.12.0

# 回测（Qbot 已依赖 backtrader）
backtrader>=1.9.76.123

# 配置 & 缓存
pyyaml>=6.0
pyarrow>=15.0            # parquet 缓存需要

# 测试
pytest>=7.4

# AI 情绪分析层（可选）：任选其一或多个，不装则 stub 模式
anthropic>=0.34.0
openai>=1.40.0

# Web 服务端（M6）
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
