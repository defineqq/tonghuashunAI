# 复制此文件为 .env 并填入你自己的密钥。.env 已被 .gitignore 忽略。

# LLM API（后续 M2 阶段 AI 分析层使用；未定服务商，二选一或都填）
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
DEEPSEEK_API_KEY=

# Tushare Pro token（可选，AkShare 用不到；如果后续接 tushare pro 拉更全的数据再填）
TUSHARE_TOKEN=

# 数据缓存路径（默认 ./data）
DATA_DIR=./data
